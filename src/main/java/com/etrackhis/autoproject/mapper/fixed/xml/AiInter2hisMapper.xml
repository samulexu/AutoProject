<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etrackhis.autoproject.mapper.fixed.AiInter2hisMapper">
 
    <resultMap type="AiInter2his" id="AiInter2hisResult">
        <result property="jkdzid" column="JKDZID" />
        <result property="wbjkid" column="WBJKID" />
        <result property="mllbid" column="MLLBID" />
        <result property="jkzdid" column="JKZDID" />
        <result property="yymlid" column="YYMLID" />
    </resultMap>
    
    <!--查询的表字段-->
    <sql id="selectVo">
        select JKDZID, WBJKID, MLLBID, JKZDID, YYMLID from AI_INTER2HIS
    </sql>
    
    <!--通过主键查询单条-->
    <select id="selectOneByKey" resultMap="AiInter2hisResult">
        <include refid="selectVo"/>
        <where>
            JKDZID = #{jkdzid}  
        </where>
    </select>
    
    <!--通过对象主键查询单条-->
    <select id="selectOne" resultMap="AiInter2hisResult">
        <include refid="selectVo"/>
        <where>
            JKDZID = #{jkdzid}  
        </where>
    </select>
    
    <!--通过Map参数根据主键查询单条-->
    <select id="selectOneByMap" resultMap="AiInter2hisResult" parameterType="java.util.HashMap">
        <include refid="selectVo"/>
        <where>
            JKDZID = #{jkdzid}  
        </where>
    </select>
    
    <!--通过实体参数查询多行-->
    <select id="selectList" resultMap="AiInter2hisResult">
        <include refid="selectVo"/>
        <where>
            1 = 1
            <if test="jkdzid != null"> and JKDZID = #{jkdzid}</if> 
            <if test="wbjkid != null and wbjkid != ''"> and WBJKID = #{wbjkid}</if> 
            <if test="mllbid != null"> and MLLBID = #{mllbid}</if> 
            <if test="jkzdid != null and jkzdid != ''"> and JKZDID = #{jkzdid}</if> 
            <if test="yymlid != null and yymlid != ''"> and YYMLID = #{yymlid}</if> 
        </where>
    </select>
    
    <!--通过Map参数查询多行-->
    <select id="selectListByMap" resultMap="AiInter2hisResult" parameterType="java.util.HashMap">
        <include refid="selectVo"/>
        <where>
            1 = 1
            <if test="jkdzid != null"> and JKDZID = #{jkdzid}</if> 
            <if test="wbjkid != null and wbjkid != ''"> and WBJKID = #{wbjkid}</if> 
            <if test="mllbid != null"> and MLLBID = #{mllbid}</if> 
            <if test="jkzdid != null and jkzdid != ''"> and JKZDID = #{jkzdid}</if> 
            <if test="yymlid != null and yymlid != ''"> and YYMLID = #{yymlid}</if> 
        </where>
    </select>
    
    <!--根据条件查询行数-->
    <select id="selectCount" resultType="java.lang.Long">
        select count(*) from AI_INTER2HIS
        <where>
            1 = 1
            <if test="jkdzid != null"> and JKDZID = #{jkdzid}</if> 
            <if test="wbjkid != null and wbjkid != ''"> and WBJKID = #{wbjkid}</if> 
            <if test="mllbid != null"> and MLLBID = #{mllbid}</if> 
            <if test="jkzdid != null and jkzdid != ''"> and JKZDID = #{jkzdid}</if> 
            <if test="yymlid != null and yymlid != ''"> and YYMLID = #{yymlid}</if> 
        </where>
    </select>
    
    <!--插入单条-->
    <insert id="insert">
        insert into AI_INTER2HIS
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="jkdzid != null">JKDZID,</if>
            <if test="wbjkid != null">WBJKID,</if>
            <if test="mllbid != null">MLLBID,</if>
            <if test="jkzdid != null">JKZDID,</if>
            <if test="yymlid != null">YYMLID,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="jkdzid != null">#{jkdzid},</if>
            <if test="wbjkid != null">#{wbjkid},</if>
            <if test="mllbid != null">#{mllbid},</if>
            <if test="jkzdid != null">#{jkzdid},</if>
            <if test="yymlid != null">#{yymlid},</if>
        </trim>
    </insert>
     
    <!--根据条件更新数据-->
    <update id="update">
        update AI_INTER2HIS
        <trim prefix="SET" suffixOverrides=",">   
            <if test="jkdzid != null">JKDZID = #{jkdzid},</if>
            <if test="wbjkid != null">WBJKID = #{wbjkid},</if>
            <if test="mllbid != null">MLLBID = #{mllbid},</if>
            <if test="jkzdid != null">JKZDID = #{jkzdid},</if>
            <if test="yymlid != null">YYMLID = #{yymlid},</if>
        </trim>
        <where>
            ROWID = #{rowId}
        </where>
    </update>
    
    <update id="updateByKey">
        update AI_INTER2HIS
        <trim prefix="SET" suffixOverrides=",">   
            <if test="wbjkid != null">WBJKID = #{wbjkid},</if>
            <if test="mllbid != null">MLLBID = #{mllbid},</if>
            <if test="jkzdid != null">JKZDID = #{jkzdid},</if>
            <if test="yymlid != null">YYMLID = #{yymlid},</if>
        </trim>
        <where>
            JKDZID = #{jkdzid}  
        </where>
    </update> 
    
    <!--根据主键删除数据-->
    <delete id="delete">
        delete from AI_INTER2HIS
        <where>
            JKDZID = #{jkdzid}  
        </where>
    </delete>
    
    <!--自定义sql-->
</mapper>
